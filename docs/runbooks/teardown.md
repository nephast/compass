# Runbook: teardown (COMPASS-38)

Run `./scripts/teardown.sh` and follow its manual verification checklist —
do not trust the script's exit code alone, confirm in the console. See the
script itself for the full checklist (EKS, NAT instance, RDS, VPC endpoints).

## Session log

Track when you tore things down and stood them back up — useful for the cost
report (COMPASS-37) and for noticing if something is costing more than expected.

| Date | Action | Notes |
|------|--------|-------|
| | | |
