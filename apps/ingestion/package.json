{
  "name": "@compass/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test:integration": "node --env-file=../../.env ../../node_modules/.bin/vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3.700.0",
    "@types/aws-lambda": "^8.10.0",
    "@types/pg": "^8.23.0",
    "esbuild": "^0.28.0",
    "jszip": "^3.10.0",
    "vitest": "^4.1.0"
  }
}
